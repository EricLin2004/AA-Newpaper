<h1> <%= @user.name %>'s Profile </h1>
<p>You are currently subscribed to the following:</p>
<ul>
<% @user.subscription_plans.each do |plan| %>
	<li>
		<%= Newspaper.find(plan.newspaper_id).title %> for $<%= plan.price %>
		<% if plan.daily %>
			per day!
		<% else %>
			per week!
		<% end %>
		<%= link_to "Unsubscribe", subscription_plan_user_url(plan), :method => :put %>
	</li>
<% end %>
</ul>